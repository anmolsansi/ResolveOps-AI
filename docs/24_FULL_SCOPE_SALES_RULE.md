# ResolveOps AI — Full Scope Sales Rule

If a paid package includes a capability, that capability must be complete before the package is sold. A missing promised capability is a launch blocker, not a reason to reduce the price. A feature that is deliberately outside the package is a future capability, not partial delivery.
